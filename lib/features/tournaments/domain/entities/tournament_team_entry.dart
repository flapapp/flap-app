/// Approved team participating in a tournament (for scheduling fixtures).
class TournamentTeamEntry {
  const TournamentTeamEntry({
    required this.teamId,
    required this.name,
  });

  final String teamId;
  final String name;
}
